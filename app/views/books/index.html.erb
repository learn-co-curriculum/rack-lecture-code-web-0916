<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Books</title>
  </head>
  <body>
    <h1>All of The Books</h1>
    <ul>
      <% @books.each do |book| %>
        <li><a href="/books?id=<%=book.id%>"><%= book.title %></a></li>
      <% end %>
    </ul>
  </body>
</html>
